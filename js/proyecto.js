const hamb = document.querySelector('.hamb');//boton

const enlaces = document.querySelector('.enlaces');//ul

hamb.addEventListener('click', () => {
    console.log(hamb);
    // enlaces.classList.toggle('activado');
});